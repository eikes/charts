#!/usr/bin/env ruby

require_relative '../lib/graph_tool'

args = ARGV
# Please leave the 'code' comments in here:
# code #
options = GraphTool::OptParser.new(args).parse
GraphTool::Dispatcher.new(options).render unless options[:help]
# code #
