#!/usr/bin/env ruby

require File.expand_path("../../lib/svg_count_graph.rb", __FILE__)

if ARGV.length == 0
  puts "Usage:"
  puts "$ bin/graph_tool Purple=33 Gold=13"
  exit
end

params = ARGV.map { |e| e.split("=") }.to_h

puts SvgCountGraph.new(params).render
