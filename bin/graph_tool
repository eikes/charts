#!/usr/bin/env ruby

require File.expand_path("../../lib/symbol_count_graph.rb", __FILE__)

if ARGV.length == 0
  puts "Usage:"
  puts "$ bin/graph_tool x=23 o=19"
  exit
end

params = ARGV.map { |e| e.split("=") }.to_h

puts SymbolCountGraph.new(params).render
